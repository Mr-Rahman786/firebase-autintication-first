import React from 'react';

const Register = () => {
    return (
        <div>
            {/* <form onSubmit={handleRegister}>
        <input onBlur={handlerEmail} type="email" name="email" id="" placeholder='input your email' />
        <br />
        <input onBlur={handlerPassword} type="password" name="password" id="" placeholder='input your password' />
        <br />
        <button type="submit">Register</button>
     </form> */}
        </div>
    );
};

export default Register;